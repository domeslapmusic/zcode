function setHeight(h){ document.getElementById("container").style.height = h+"px"; }
function updateTitle(s){ document.title = s; }
function resetScroll(){ window.scrollTo(0,0); }
function setScrollPosition(y){ /*window.scrollTo(0,y);*/ }
function reloadPage(){ window.location.reload(); }
function changeStatus(s){ window.status = s; }